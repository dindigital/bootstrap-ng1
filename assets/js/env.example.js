/**
 * @description
 * Variáveis de ambiente.
 *
 * TODO: renomeie para env.js, atualize o path das rotas e remova essa linha.
 */

(function (window) {
    window.__env = window.__env || {};
    window.__env.URL_API = 'https://randomuser.me/';
}(this));
